<div class="flex flex-col justify-center items-center min-h-[95%] text-xl gap-2" data-controller="notice">
  <%= render 'partials/navbar' %>
  <%= form_with url: "/analytics", method: :get, class: "flex w-full gap-2 justify-center py-2 px-6 md:w-[768px]" do |form| %>
    <%= form.url_field :url, required: true, class: "flex-1 bg-gray-100 rounded-lg border-transparent focus:border-transparent focus:ring-0 autofill:bg-transparent", placeholder: "https://snippit.my/t______" %>
    <%= form.submit value: "get analytics", class: "p-2 rounded-lg cursor-pointer font-medium text-base bg-darkslategrey text-vanilla border border-vanilla transition hover:bg-vanilla hover:text-darkslategrey" %>
  <% end %>
  <%= render 'partials/alert'%>
  <div class="flex gap-5">
    <% if current_user %>
      <%= link_to "your links", user_path(current_user), class: "text-cream cursor-pointer hover:underline hover:text-vanilla font-medium" %>
    <% end %>
    <%= link_to "shortener", home_path, class: "text-cream hover:text-vanilla cursor-pointer hover:underline font-medium" %>
    <% if current_user %>
      <%= button_to "log out", destroy_user_session_path, method: :delete, class: "text-cream cursor-pointer hover:underline hover:text-vanilla font-medium" %>
    <% else %>
      <%= link_to "log in", new_user_session_path, class: "text-cream cursor-pointer hover:underline hover:text-vanilla font-medium", data: { action: "mouseover->notice#reveal" } %>
      <%= link_to "sign up", new_user_registration_path, class: "text-cream cursor-pointer hover:underline hover:text-vanilla font-medium", data: { action: "mouseover->notice#reveal" } %>
    <% end %>
  </div>
  <% unless current_user %>
    <p class="text-darkslategrey font-medium my-5" data-notice-target="reason">sign up to track your links!</p>
  <% end %>
</div>
