<%= form_with model: url do |form| %>
    <%= form.text_field :target %>
    <%= form.submit value: "Shorten URL", class: "bg-blue-500 text-white p-2 border border-blue-600 hover:bg-white hover:text-blue-500 cursor-pointer" %>
<% end %>
